# Sky Striker：AI 辅助开发工作流程记录

**项目目标：** 使用 Phaser 制作一款可在浏览器中运行的 2D 纵向飞机射击游戏，并将 AI 视觉生成、程序化效果、游戏代码与视觉验证串成可复用的生产闭环。

> 这份记录强调“AI 产物必须经过可玩验证”。生成图片并不是完成；它们只有在游戏里具备正确尺寸、可读性、性能表现与明确的交互职责后，才是可用资产。

## 1. 从简报到可验证范围

项目将首版限制为单人、单战斗场景、单一核心循环。玩家四向机动，战机自动发射子弹；敌机向下压迫并反击；击落目标累计得分与连击；装甲耗尽时进入结算并能即时重开。账号、商业化、联机和复杂剧情均被排除，避免在玩法尚未验证前扩大开发面。

| 决策 | 实施方式 | 验证方式 |
|---|---|---|
| 画面方向 | 选择“赤道雷暴行动”：复古未来主义航空插画、深蓝绿雷暴、青绿友方信息、琥珀能量读数。 | 对照 `ideas.md` 与自动演示截图检查色彩、镜头和信息层级。 |
| 游戏架构 | React 负责唯一的 Phaser 生命周期容器；Phaser 场景拥有所有战斗规则、物理组、计时器和 HUD。 | 刷新页面与重新进入场景时只保留一个画布，控制台无重复初始化错误。 |
| 技术风险 | 将对象池、碰撞无敌时间、场景状态切换和自动演示作为风险切片先实现。 | 通过 `PLAN.md` 中列出的输入、得分、受击、重启和视觉检查逐项验证。 |

## 2. AI 视觉资产的生成与接入

先生成一张包含玩家机、敌机、子弹、背景和 HUD 的游戏内画面参考；它限定真实要实现的对象，而不是只提供氛围概念。随后生成了雷暴背景、玩家截击机、敌方无人截击机和无文字品牌图标。小尺寸且高频的子弹、护盾、爆炸、雨线使用 Phaser 图形 API 绘制，以保持清晰度并减少纹理切换。

生成资源通过托管 URL 在运行时载入，并在 `ASSETS.md` 中记录路径、用途与游戏内显示尺寸。这个分层把生成原图与最终运行时资源区分开来：玩家与敌机是叙事与识别度较高的生成资产；子弹和特效是规则明确、适合程序化的动态资产。

| 资产类别 | 生成/制作策略 | 在游戏中的职责 |
|---|---|---|
| 雷暴背景 | AI 生成具有中央航道和两侧云层的竖向构图。 | 双背景交替下移，形成稳定的纵向推进感。 |
| 玩家与敌机 | AI 生成透明底、顶视角、强剪影的独立精灵。 | 提供友敌辨识，分别固定为 80×80 px 和 64×64 px。 |
| HUD 徽记 | AI 生成不含文字的三角翼/航迹符号。 | 菜单徽记与浏览器图标，确保品牌在小尺寸仍能识别。 |
| 高频特效 | 程序化纹理和 tween。 | 子弹、护盾、爆炸与雨线，以对象池和短生命周期运行。 |

## 3. Phaser 实现顺序

实现遵循风险优先策略。首先建立 `GameCanvas`，保证 React 卸载时主动销毁 Phaser 实例；随后配置 Arcade Physics、固定 16:9 画布和三个场景。`BootScene` 预载托管图片并生成程序化纹理；`MenuScene` 负责入场与品牌呈现；`BattleScene` 是战斗状态的唯一拥有者，管理玩家、敌机、子弹、补给、碰撞和 HUD。

战斗场景使用对象池复用敌机、玩家子弹、敌方子弹与补给。当实体越过战区边界时会被禁用并回收，而不是持续创建新对象。玩家与敌机的碰撞通过短暂无敌时间和 `active` 检查避免重复结算。死亡后，生成计时器被停止，游戏展示结算面板并允许按 Enter 或空格重新起飞。

| 开发切片 | 具体完成内容 | 为什么先做 |
|---|---|---|
| 生命周期切片 | 单一 Phaser 实例、窗口缩放与卸载销毁。 | 避免开发时的重复画布、重复输入和内存问题。 |
| 基础战斗切片 | 四向机动、自动开火、敌机生成、双向弹体与 Arcade Physics。 | 用最小闭环验证“移动—射击—躲避”是否成立。 |
| 状态切片 | 计分、连击、装甲、护盾、暂停、死亡与重开。 | 把短局街机体验补成完整可重复流程。 |
| 展示切片 | 雷暴滚动背景、雨线、边缘 HUD、菜单和移动端控制。 | 使玩法信息与视觉方向相互支撑，而不是后期贴图。 |

## 4. 验证流程与结果

静态检查使用 `pnpm check`，生产构建使用 `pnpm build`；两者均已通过。运行时验证使用 `?demo` 自动演示：该模式让玩家机做确定性横移，并在场景初始阶段安排可见敌机编队。这样截图能显示实际的玩家机、敌机、上行青绿弹、雷暴背景、装甲与得分 HUD，而不是静态菜单或空画布。

| 检查项 | 结果 | 证据/位置 |
|---|---|---|
| 类型检查 | 通过。 | `pnpm check`。 |
| 生产构建 | 通过。 | `pnpm build`。 |
| 生成资源载入 | 通过，背景、玩家机、敌机与图标均使用托管 URL。 | `BootScene.ts`、`ASSETS.md`。 |
| 自动战斗画面 | 通过，截图可见玩家机、三架敌机、玩家子弹、云层雨线与顶部 HUD。 | `/?demo=1&fresh=2`。 |
| 桌面输入 | 已实现方向键/WASD，自动开火，P 键暂停。 | `BattleScene.ts` 的输入与状态逻辑。 |
| 移动输入 | 已实现四向触控控制（左横右纵）；HUD 采用边缘布局。 | `BattleScene.ts` 的 `createTouchControls`。 |

最终视觉检查进一步强化了航电辨识逻辑：开始动作、底部仪表带与触控控制器改为切角几何；画面边缘添加航线、距离和威胁扫描读数；敌机统一增加暖朱红/琥珀色调，使其在低对比雷暴背景中保持可见。相关决策已同步至 `ideas.md` 的“Style Decisions”。

## 5. 复现与后续迭代

本地开发时，在项目根目录执行 `pnpm dev` 后访问首页；附加 `?demo=1` 可以进入自动演示。后续如果要扩展关卡或敌机类型，建议先在 `PLAN.md` 写下新增对象的显示层级、碰撞规则、对象池上限和截图中的验证条件，再生产资产。不要直接复制现有敌机并堆叠数值，因为新类型必须给玩家带来可观察的走位或射击决策。

可优先迭代的方向是：为精英敌机加入一种清晰可躲的扇形弹幕、增加可选的临时武器升级、为爆炸加入更丰富但不遮挡信息的粒子层，以及在真实移动设备上验证触控按钮的实际手感。每一个方向应独立经过“短实现 → 自动演示截图 → 手动试玩 → 再扩展”的循环。

## 6. 本地交接加固记录

为满足可脱离 WebDev 环境继续开发的要求，四张生成 PNG 已从托管资源复制到 `client/public/assets/`，并将 `BootScene` 和 favicon 改为只使用 `/assets/` 本地路径。随后在一个删除 `node_modules` 的干净副本中执行 `pnpm install --frozen-lockfile`、`pnpm check` 与 `pnpm build`；三项均通过。该副本再以独立端口启动 Vite，`?demo=1` 返回 HTTP 200，证明交接包具备独立安装、构建和启动的基础条件。

交接前还处理了代码审查中的五项核心问题：暂停事件精确解除、敌机漂移接入、暂停冻结 Tween、敌方错峰瞄准弹，以及可扩展的分数格式。处理前后的事实、剩余限制与 v0.02 优先级均归档在 `KNOWN_ISSUES.md`。
