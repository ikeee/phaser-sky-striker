# Game Plan: Sky Striker

## 项目范围

Sky Striker 是一款单人、浏览器内运行的 2D 纵向卷轴飞机射击游戏。玩家驾驶拦截机在雷暴云层中左右机动，自动持续开火，击落来袭截击机并收集护盾能量。首版聚焦短局街机循环：**启动任务 → 躲避/射击 → 计分与连击 → 受击或清屏 → 重开挑战**。桌面端支持方向键或 WASD；移动端支持屏幕按钮。游戏包含开始屏、暂停、生命值、得分、连击、护盾与结算重开；不包含账号、付费、联机、商店、剧情分支或外部数据服务。

## Risk Tasks

### 1. Phaser 与 React 生命周期集成

- **Why isolated:** React 19 的开发模式可能重复挂载组件；若 Phaser 实例或键盘监听未被销毁，画布会叠加、输入会重复并且内存持续增长。
- **Approach:** 将 `Phaser.Game` 封装为独立 `GameCanvas` 生命周期组件；只在稳定容器创建一个实例，卸载时显式 `destroy(true)`，所有场景逻辑保持在 `client/src/game/` 下并与 React UI 解耦。
- **Verify:** 开发环境刷新、暂停/重启、路由再次挂载后只存在一个画布；键盘与触屏开火不会重复；控制台无 Phaser 容器或监听器相关报错。

### 2. 纵向卷轴、敌机生成与对象池

- **Why isolated:** 纵向推进、持续刷怪和重复子弹是首版中最容易产生对象泄漏、帧率下降或越界碰撞的动态系统。
- **Approach:** 使用 Phaser Arcade Physics 的组与对象池管理玩家弹、敌方弹与敌机；所有对象越过边界后失活并回收。背景使用双层可重复移动贴图或 Canvas 纹理，固定以每秒速度向下滚动。
- **Verify:** 运行 90 秒后敌机和弹体数量处于稳定范围，画面无静止残留物；对象到达边界后正确回收；云层、雨线和敌机均持续向下推进。

### 3. 碰撞、受击无敌与游戏状态切换

- **Why isolated:** 同一帧内多枚子弹可能重复命中玩家或敌机，进而造成扣血连跳、计分翻倍或死亡后仍能操作。
- **Approach:** 为玩家与敌机设置短暂受击冷却；碰撞回调只处理活跃实体；死亡后停止生成、关闭输入，转入结算覆盖层；重开时重置所有组、计时器和 HUD。
- **Verify:** 敌弹与玩家接触只减少一格装甲并触发一次闪烁；敌机被子弹击中只计一次分；清空生命后画面停止战斗并可以无刷新重开。

## Main Build

游戏主循环由 `BootScene`、`MenuScene` 与 `BattleScene` 组成。菜单进入战斗后，玩家可左右移动并自动射击；普通敌机会沿轻微摆动航线向下压迫，精英敌机会更慢且更耐久。击落敌机增加分数和连击；连击阈值提高分数倍率；护盾拾取物恢复一格装甲。游戏保留键盘、鼠标/触摸与暂停控制，并在 `?demo` 参数下启用可预测的自动驾驶，便于验证截图中展示真实战斗。

- **Assets:** `storm_background` 作为纵向滚动云海；`player_interceptor` 作为 80×80 px 玩家机；`enemy_drone` 作为 64×64 px 敌机（精英 82×82 px）；`sky_striker_mark` 用于开始屏与 HUD；`player_bullet`、`enemy_bullet`、`shield_pickup`、`explosion`、`rain_streak` 由 Phaser 图形 API 生成并复用。完整规格见 `ASSETS.md`。
- **Verify:**
  - 玩家横移方向与键盘、鼠标/触控位置一致；在边界处不会飞出可视战区。
  - 自动开火持续产生向上飞行的青绿弹体；敌方弹体方向与碰撞规则准确。
  - 击落敌机后分数和连击立即变化；护盾拾取物能恢复装甲。
  - HUD 在桌面与移动宽度下清晰、不溢出、不遮挡核心战区；暂停和重启可达。
  - 无缺失贴图、无明显占位色块、无控制台报错；减少动态效果偏好下不执行非必要装饰运动。
  - 画面与 `sky-striker-reference` 保持一致：直接俯视、深海蓝绿雷暴、青绿友方信息、琥珀能量读数、画面密度适中。
  - **Presentation proof bundle:** 通过 WebDev 预览截图记录 `?demo` 的实际战斗状态，并以 `pnpm check` 通过作为静态检查。
